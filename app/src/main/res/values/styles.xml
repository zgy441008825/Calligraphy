<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="styleMainStatisticsNumber">
        <item name="android:textSize">@dimen/sp_26</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="styleMainStatisticsInfo">
        <item name="android:textSize">@dimen/sp_12</item>
        <item name="android:textColor">@android:color/darker_gray</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="DefaultBt">
        <item name="android:background">@drawable/bt_bg_main</item>
        <item name="android:textColor">@color/main_bt_default</item>
        <item name="android:textSize">@dimen/sp_16</item>
    </style>

</resources>
